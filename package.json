{
  "name": "agentapi",
  "version": "0.10.0",
  "description": "Multi-tenant AgentAPI with MCP support",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
